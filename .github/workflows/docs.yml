---
name: Deploy docs to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/docs.yml
      - docs/**
      - packages/vscode-sveltex/syntaxes/**
      - scripts/docs/**
  workflow_dispatch:

permissions:
  contents: read

# One docs deployment at a time; let an in-progress run finish rather
# than cancelling it mid-deploy.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    name: Build the VitePress site
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      # Audit outbound traffic during the build (fetches grammars, fonts,
      # twoslash typedoc data, etc.).
      - uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411 # v2.19.4
        with:
          egress-policy: audit

      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          # VitePress `lastUpdated` reads each page's last-commit time.
          fetch-depth: 0

      - uses: pnpm/action-setup@0e279bb959325dab635dd2c09392533439d90093 # v6.0.8
        name: Install pnpm

      - name: Install Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version-file: docs/.node-version
          cache: pnpm
          cache-dependency-path: |
            pnpm-lock.yaml
            docs/pnpm-lock.yaml

      # `docs/` is a standalone project — not a pnpm-workspace member. Its own
      # `docs/pnpm-workspace.yaml` makes it a self-contained workspace root
      # (carrying its build-script allow-list), so a plain `pnpm install` here
      # resolves `docs/pnpm-lock.yaml` without climbing to the monorepo root.
      - name: Install dependencies
        run: pnpm install
        working-directory: docs

      # `update-stats.mjs` recomputes the project-stat figures quoted in
      # `index.md` (via `cloc` and `vitest list` — hence the workspace install
      # and `cloc` here) so the published site always shows current numbers.
      # Nothing is committed back; a stats glitch must not block the deploy.
      - name: Install workspace dependencies
        run: pnpm install

      - name: Install cloc
        run: sudo apt-get update && sudo apt-get install --yes cloc

      - name: Refresh the project-stat figures
        run: node scripts/docs/update-stats.mjs
        continue-on-error: true

      # `pnpm build` runs `build:playground`, then `vitepress build`. The
      # VitePress config reads grammars from `../packages/vscode-sveltex`,
      # so the whole repository must be checked out (it is).
      - name: Build the docs site
        run: pnpm build
        working-directory: docs

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9 # v5.0.0
        with:
          path: docs/.vitepress/dist

  deploy:
    name: Deploy to GitHub Pages
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 10

    permissions:
      pages: write
      id-token: write

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      # Audit -- this job holds the `id-token: write` permission for
      # GitHub Pages deployment via OIDC, so any unexpected egress is
      # worth a record.
      - uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411 # v2.19.4
        with:
          egress-policy: audit

      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@cd2ce8fcbc39b97be8ca5fce6e763baed58fa128 # v5.0.0
