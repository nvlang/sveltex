---
name: Test and Release

on:
  push:
    branches:
      - main
    paths:
      - packages/*/src/**
      - packages/*/pnpm-lock.yaml
      - packages/*/tsconfig.json
      - packages/*/tsconfig.*.json
      - packages/*/package.json
      - packages/*/jsr.json
      - packages/*/README.md
      - packages/*/LICENSE
      - packages/*/CHANGELOG.md
  workflow_dispatch:

permissions:
  contents: write
  statuses: write
  id-token: write
  pull-requests: write

# pnpm 11 hard-errors on dependency build scripts that aren't allow-listed.
# Some CI installs run outside the workspace — the e2e `_template`, and the
# throwaway projects `@nvl/sv`'s test harness scaffolds — and so can't see the
# root `pnpm-workspace.yaml` allow-list. Allow all build scripts in CI, where
# the lockfile already pins every dependency.
env:
  pnpm_config_dangerously_allow_all_builds: 'true'

jobs:
  publish:
    name: Publish to NPM and JSR
    runs-on: ubuntu-latest
    # Runs as a deployment to the `release` environment so npm trusted
    # publishing (OIDC) can match the publish against this environment.
    environment: release
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0 # Fetch all history for all branches and tags

      - name: Set up TeX Live
        uses: TeX-Live/setup-texlive-action@v3
        with:
          packages: |
            scheme-basic
            luatex
            xetex
            latexmk
            dvisvgm
            standalone
            pgf
            microtype
            mathtools
            luatex85
            lipsum
            xcolor

      - uses: pnpm/action-setup@v4
        name: Install pnpm
        with:
          run_install: false

      - name: Install Node.js
        uses: actions/setup-node@v6
        with:
          node-version: lts/*
          cache: pnpm
          cache-dependency-path: ./pnpm-lock.yaml
          registry-url: 'https://registry.npmjs.org'

      - name: Install Dependencies
        run: pnpm install

      - name: Install Poppler
        run: sudo apt-get install -y poppler-data poppler-utils

      - name: Install MuPDF
        run: sudo apt-get install -y mupdf mupdf-tools

      - name: Build
        run: pnpm build

      - name: Run Vitest unit tests
        run: pnpm test

      - name: Upload coverage reports for `packages/sveltex` to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          directory: packages/sveltex/coverage/
          flags: sveltex

      - name: Upload Vitest report for `packages/sveltex` to GitHub
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: vitest-report
          path: packages/sveltex/html/
          retention-days: 30

      - name: Determine current branch
        run:
          echo "BRANCH_NAME=$(git rev-parse --abbrev-ref HEAD)" >> $GITHUB_ENV

      - name: Ensure current branch is main
        run: |
          if [ "$BRANCH_NAME" != "main" ]; then
            echo "Not on main branch, exiting."
            exit 1
          fi

      # When you push something to main, this action will first create a PR on a
      # new branch (`changeset-release/main`), where it'll have applied the
      # version bumps and changelog edits and such. Then, once you merge that
      # PR, it'll rerun this action, but this time it'll be publishing the
      # changes instead of creating a PR.
      - name: Publish to NPM and JSR or create release PR
        id: changesets
        uses: changesets/action@v1
        with:
          branch: main
          version: pnpm ci:version
          publish: pnpm ci:publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Push git tags
        # Don't run this step if the changesets action merely created a PR,
        # since otherwise we'll get an error about the branch not having an
        # upstream branch.
        if: steps.changesets.outputs.published == 'true'
        run: git push --follow-tags

      - name: Publish the VS Code extension
        # The extension (`packages/vscode-sveltex`) is excluded from the npm
        # publish above, so it needs its own publish step. `changeset tag`
        # (run by `ci:publish`) created a `sveltex@<version>` tag at HEAD only
        # if the extension's version was bumped this release cycle — when it
        # was, package the extension once and publish that one `.vsix` to both
        # the VS Code Marketplace and Open VSX (the registry used by VSCodium,
        # Cursor, Windsurf, Gitpod, …).
        if: steps.changesets.outputs.published == 'true'
        working-directory: packages/vscode-sveltex
        run: |
          if git tag --points-at HEAD | grep -q '^sveltex@'; then
            echo 'Extension released this cycle — packaging and publishing.'
            pnpm dlx @vscode/vsce@3 package --no-dependencies -o sveltex.vsix
            pnpm dlx @vscode/vsce@3 publish --packagePath sveltex.vsix
            pnpm dlx ovsx publish sveltex.vsix \
              || echo '::warning::Open VSX publish failed — publish sveltex.vsix manually.'
          else
            echo 'Extension version unchanged — nothing to publish.'
          fi
        env:
          VSCE_PAT: ${{ secrets.VSCE_PAT }}
          OVSX_PAT: ${{ secrets.OVSX_PAT }}
