
# Code: Basic

Some syntax highligthing for TS (won't be colorful if the `codeBackend` is set to `'escape'`):


```ts
const something = 'something';
```

Now, let's try some _inline_ syntax highlighting (again, won't be colorful if the `codeBackend` is set to `'escape'`):

`ts const something = 'something';`

