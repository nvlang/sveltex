import { describe, expect, test } from 'vitest';

describe('example', () => {
    test('law of the excluded middle', () => {
        expect(!true).toBe(false);
        expect(!false).toBe(true);
    });
});
